---
id: 44
status: Completed
created: 2026-03-23
phase_current: 3
phase_completed: [1, 2, 3]
agents:
  - dev-agent
  - frontend-agent
---

# CRM Emiralia — B2B Sales & Lead Management

## Problema

Emiralia tiene modelo de negocio B2B (developers pagan subscripción + comisión por lead), pero no existe ninguna infraestructura para ejecutarlo:

1. **Bug crítico:** el formulario del website hace `POST /api/leads` pero ese endpoint no existe → todos los leads de inversores se pierden con 404.
2. **0 gestión de cuentas B2B:** no hay tabla `developers`, no hay forma de trackear qué promotores están en proceso de venta, ni cuáles son clientes activos.
3. **0 tracking de revenue:** no hay tabla `subscriptions`, no hay forma de calcular MRR ni comisiones pendientes.
4. **0 pipeline comercial:** no hay lugar donde gestionar la adquisición de developers (desde prospecto hasta contrato cerrado).

## Solución

Construir un CRM nativo en el dashboard que cubra los dos pipelines del modelo de negocio:

1. **Pipeline de Leads (inversores):** capturar, cualificar y asignar leads de inversores a developers (fuente de comisiones).
2. **Pipeline B2B (developers):** gestionar la adquisición de developers como clientes pagadores, desde prospecto hasta subscripción activa.
3. **Revenue tracking:** MRR de subscriptions + comisiones pendientes/pagadas.
4. **Log de comunicaciones:** historial de interacciones con developers y leads.

### Arquitectura

```
Website forms → POST /api/leads → leads table → Lead Management Dashboard
                                               ↓ qualify → assign
                                          Developer accounts
                                          Subscriptions → MRR
                                          Lead assignments → Commissions

Outbound sales → deals pipeline → closed_won → developer.tier = 'active'
```

**Patrón seguido:** exactamente igual que Campaign Manager (044) — migration SQL + route module factory + page component.

## Métricas de Éxito

- `POST /api/leads` funcional (0 leads perdidos desde el website)
- Dashboard CRM visible en `/dashboard/crm` con 5 tabs
- Pipeline kanban de deals operativo (7 stages)
- Crear un developer prospect y moverlo a `active` vía deal `closed_won`
- `GET /api/crm/metrics` retorna MRR, ARR, pipeline value, leads by status
- 0 librerías externas para drag-and-drop (state local optimista)

## Arquitectura WAT

### Tablas DB nuevas

| Tabla | Propósito |
|-------|-----------|
| `developers` | Cuentas B2B (prospect/trial/active/churned) |
| `subscriptions` | Planes activos por developer (MRR) |
| `deals` | Pipeline comercial 7 stages |
| `communications` | Log de interacciones (email/WhatsApp/call/meeting/note) |
| `developer_projects` | Proyectos que un developer lista en Emiralia |
| `lead_assignments` | Asignación lead → developer para comisiones |

### ALTER TABLE leads

Añadir: `status` (captured→qualifying→qualified→assigned→converted→lost), `qualified_by`, `deal_value`, `assigned_developer_id`.

### Nuevos endpoints API

```
POST   /api/leads                      — captura desde website (repara bug crítico)
GET    /api/leads                      — listar leads con filtros
PATCH  /api/leads/:id                  — actualizar status/qualify
POST   /api/leads/:id/assign           — asignar a developer

GET/POST  /api/crm/developers          — cuentas B2B CRUD
GET/PATCH /api/crm/developers/:id      — perfil + subscription activa + proyectos

GET/POST  /api/crm/deals               — pipeline comercial CRUD
PATCH     /api/crm/deals/:id/stage     — mover deal (con validación de transiciones)

POST/PATCH /api/crm/subscriptions      — gestión de planes
GET/POST   /api/crm/communications     — log de interacciones
GET        /api/crm/metrics            — MRR, ARR, pipeline value, leads by status
```

### Lógica de transición de deals

```
prospecting → contacted → demo_scheduled → proposal_sent → negotiation → closed_won/lost
```

`closed_won` → actualiza `developers.tier = 'active'` + crea communication log automático.
`closed_lost` → require `lost_reason` en body.

## Fases y Tareas

### Fase 1 — DB Foundation

**Agentes:** dev-agent | **Prioridad:** Critical

| Task | Descripción | Agente | T | Estado |
|------|-------------|--------|---|--------|
| 1.1 | `tools/db/migration_crm.sql`: tablas developers, subscriptions, deals, communications, developer_projects, lead_assignments + ALTER leads | dev-agent | M | Todo |
| 1.2 | Correr migración y verificar 6 tablas creadas + columna leads.status | dev-agent | XS | Todo |

**Verificación:**
```bash
# Confirmar tablas creadas
psql $DATABASE_URL -c "SELECT table_name FROM information_schema.tables WHERE table_name IN ('developers','subscriptions','deals','communications','developer_projects','lead_assignments');"
# Confirmar columna status en leads
psql $DATABASE_URL -c "SELECT column_name FROM information_schema.columns WHERE table_name='leads' AND column_name='status';"
```

---

### Fase 2 — API Layer

**Agentes:** dev-agent | **Prioridad:** Critical

| Task | Descripción | Agente | T | Estado |
|------|-------------|--------|---|--------|
| 2.1 | `apps/dashboard/routes/crm.js`: exportar `createLeadsRouter` (monta en /api/leads) y `createCrmRouter` (monta en /api/crm) | dev-agent | L | Todo |
| 2.2 | Implementar endpoints leads: GET/POST /api/leads, PATCH /api/leads/:id, POST /api/leads/:id/assign | dev-agent | M | Todo |
| 2.3 | Implementar endpoints developers: GET/POST + GET/PATCH/:id + GET/:id/leads | dev-agent | M | Todo |
| 2.4 | Implementar endpoints deals: GET/POST + PATCH/:id + PATCH/:id/stage (con validación transiciones) | dev-agent | M | Todo |
| 2.5 | Implementar endpoints subscriptions, communications, metrics (CTE query) | dev-agent | M | Todo |
| 2.6 | Montar routers en `server.js` + reiniciar servidor | dev-agent | XS | Todo |

**Verificación:**
```bash
curl -X POST http://localhost:3001/api/leads -H "Content-Type: application/json" \
  -d '{"name":"Test","email":"test@test.com","source":"interes","country":"Spain","interests":["dubai"]}'
# Esperar: 201 Created

curl http://localhost:3001/api/crm/metrics
# Esperar: JSON con revenue, leads, developers, pipeline
```

---

### Fase 3 — CRM Dashboard UI

**Agentes:** frontend-agent | **Prioridad:** High

| Task | Descripción | Agente | T | Estado |
|------|-------------|--------|---|--------|
| 3.1 | `CrmMetricsBar.jsx`: 4 pills (MRR, ARR, Pipeline, Leads esta semana) desde /api/crm/metrics | frontend-agent | S | Todo |
| 3.2 | Tab Leads: `LeadsTable.jsx` con columnas #/Nombre/Email/País/Fuente/Status/Fecha/Acciones + filtros status+source+search | frontend-agent | M | Todo |
| 3.3 | Tab Developers: `DeveloperGrid.jsx` con DeveloperCards (tier badge, MRR, leads asignados) | frontend-agent | M | Todo |
| 3.4 | Tab Pipeline: `PipelineKanban.jsx` — 7 columnas, deal cards con probabilidad barra, mover via PATCH/:id/stage, state optimístico | frontend-agent | L | Todo |
| 3.5 | Tab Revenue: metric cards MRR/ARR/Comisiones + SubscriptionTable + CommissionsTable | frontend-agent | M | Todo |
| 3.6 | Tab Communications: `CommTimeline.jsx` — cronológica, agrupada por día, iconos por canal | frontend-agent | M | Todo |
| 3.7 | CSS: `.kanban-board`, `.kanban-column`, `.comm-timeline` en `index.css` (~50 líneas) | frontend-agent | S | Todo |
| 3.8 | Ruta `/crm` en `main.jsx` + nav entry en `Layout.jsx` + clave traducción en `translations.js` | frontend-agent | XS | Todo |

**Verificación:**
```
http://localhost:5173/dashboard/crm
- 5 tabs cargando sin errores
- Leads tab: muestra el lead de test creado en Fase 2
- Pipeline tab: 7 columnas vacías sin errores JS
- Revenue tab: MRR $0 (sin subscriptions todavía)
```

---

## Archivos a Crear

```
tools/db/migration_crm.sql
apps/dashboard/routes/crm.js
apps/dashboard/src/pages/CRM.jsx
apps/dashboard/src/components/crm/CrmMetricsBar.jsx
apps/dashboard/src/components/crm/LeadsTable.jsx
apps/dashboard/src/components/crm/DeveloperGrid.jsx
apps/dashboard/src/components/crm/DeveloperCard.jsx
apps/dashboard/src/components/crm/PipelineKanban.jsx
apps/dashboard/src/components/crm/DealCard.jsx
apps/dashboard/src/components/crm/RevenueView.jsx
apps/dashboard/src/components/crm/CommTimeline.jsx
```

## Archivos a Modificar

```
apps/dashboard/server.js         — importar + montar createLeadsRouter + createCrmRouter
apps/dashboard/src/main.jsx      — añadir ruta /crm
apps/dashboard/src/components/Layout.jsx     — añadir nav entry CRM
apps/dashboard/src/i18n/translations.js      — clave layout.crm
apps/dashboard/src/index.css     — estilos kanban + timeline
```

## Post-MVP / Hoja de Ruta

- **Sales Agent (Proyecto 046):** IA que gestiona el outreach a developers (genera emails, busca prospectos, actualiza deals automáticamente)
- **Customer Success Agent:** onboarding automatizado de developers nuevos
- **Financial Agent:** invoicing, revenue recognition, churn prediction
- **Developer Portal:** portal self-service para developers (ver sus leads, analytics de sus proyectos)
- **Integración Stripe:** subscriptions con billing automático
- **Lead Scoring:** ML model para predecir probabilidad de conversión de lead → venta
